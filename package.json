{
  "name": "tickets-monorepo",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "cd backend && npm ci && npm run db:generate && npm run build",
    "start": "cd backend && npx prisma migrate deploy && npm start"
  }
}
